version: 0.2
    
phases:
  install:
    commands:
      - echo Installing app dependencies...
  pre_build:
    commands:
      - echo Entered the pre_build phase...
  build:
    commands:
      - echo Entered the build phase...
      - kubectl get nodes
      - helm version
      - echo test pipeline
